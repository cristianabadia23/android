<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a3enraya">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".vmachine.DificilActivity"></activity>
        <activity android:name=".vmachine.FacilActivity" />
        <activity android:name=".ui.EndGameActivity" />
        <activity android:name=".vmachine.MachineActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".ui.RankingActivity" />
        <activity android:name=".ui.GameActivity" />
        <activity
            android:name=".app.MyApp"
            android:label="@string/title_activity_game"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.EncontrarPartidaActivity" />
        <activity android:name=".ui.RegistroActivity" />
        <activity android:name=".ui.LoginActivity">

        </activity>
    </application>

</manifest>